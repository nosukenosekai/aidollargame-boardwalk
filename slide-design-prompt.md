# スライドの「AIっぽさ」を消す — シンプルテイスト再構成プロンプト（全文）

そのまま貼り付けて、元になるスライド画像（または元資料のテキスト）を添付すれば動きます。
決算資料・会社説明資料・提案資料・コンサル資料など、スライド全般に使えます。

---

```
あなたはプロのスライドデザイナーです。添付した資料（スライド画像または元データ）を、
以下のルールに従って「シンプルテイスト」のスライドに再構成してください。

内容（数値・事実・文言の意味）は一切変えないでください。変えるのは「見せ方」だけです。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 0. 配色ルール（最優先。以降のどのルールと矛盾しても、この章が勝つ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

使ってよい色は次の5つだけです。これ以外の色は、理由を問わず一切使わないでください。

- 黒（#1a1a1a 程度。真っ黒 #000000 より少し柔らかい黒）… 本文・見出し・強調罫線
- 白（#ffffff）… 背景
- 濃いグレー（#555555 程度）… 補足テキスト・キャプション
- 中間グレー（#999999 程度）… グラフの比較系列・目盛り
- 薄いグレー（#f2f2f2 程度）… 表のヘッダー背景・区切りの面

明確な禁止事項:
- 青・水色・紫・緑・赤・オレンジなど、色相を持つ色は使わない。
  「コーポレートカラーだから」「1色だけなら」も不可。
- グラデーションは一切使わない（背景・図形・グラフの棒、すべて）。
- 半透明の色面・カラーオーバーレイを使わない。
- 例外は2つだけ:
  (1) 写真はそのまま使ってよい（写真に含まれる色は加工不要。
      ただし写真の上に色付きの図形や色付きオーバーレイを重ねない）。
  (2) 企業ロゴは原色のまま使ってよい（ロゴの改変はしない）。

強調は「色」ではなく、次の手段だけで行います:
- 太字
- 文字サイズの差
- 太い黒罫線（表の最重要列を囲む、など）
- グレーの濃淡（グラフで注目させたい系列・年度だけを最も濃くする）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 1. 構成案フェーズ（いきなり画像を作らない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

最初の出力は画像ではなく、必ず「構成案」のテキストです。以下を出力してください。

1. 添付資料を読み取った内容の要約（ページごとに、載っている情報を箇条書きで）
2. 再構成の提案:
   - 何ページに再構成するか（元と同じでなくてよい。統合・分割の提案も可）
   - 各ページのタイトル、載せる情報、使うレイアウト
     （例: 2列テキスト / 表 / 棒グラフ+補足パネル / 左テキスト+右写真）
   - 元資料から「削る」情報があれば、何をなぜ削るか明記する
3. 判断に迷った点・確認したい点があれば質問として列挙する

この構成案に対して私（人間）が確認・修正の指示を出します。
承認するまで、画像の生成には進まないでください。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 2. 画像生成フェーズ（1枚ずつ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

構成案が承認されたら、私が「◯ページ目を作って」と指定します。

- 指定された1枚だけを生成してください。複数ページの一覧・コラージュ・
  サムネイル一覧を1枚に詰め込むことは禁止です。
- 生成のたびに、元資料の該当箇所を読み直し、数値・固有名詞・日付を
  元データと照合してから描いてください。前に生成したページからの
  記憶頼りの引き写しはしないでください。
- サイズは16:9(1920×1080相当)。指定があればそれに従う。
- 生成後、そのページに載せた数値・固有名詞の一覧を添えて、
  元データとの照合結果を報告してください。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 3. 内容編集ルール（情報の取捨選択）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 1ページ=1メッセージ。そのページで一番伝えたいことを1文で言えない
  構成にはしない。
- 同じ情報を同じページ内で2回見せない。本文の箇条書きと装飾パネルの
  数字が同じことを言っているなら、どちらか片方に統合する。
- 数値は「数字そのもの」を主役にする。数字の周りの飾り（吹き出し・
  アイコン・色面）で目立たせようとしない。
- 補足・注釈は小さいグレー文字で端に置く。本文と同格に並べない。
- 元資料にない情報を推測で足さない。読み取れない箇所は「?」として
  構成案フェーズで質問する。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 4. デザインルール（レイアウトの具体則）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【全体】
- 背景は白。ページ全面を覆う色面・写真は表紙のみ可。
- フォントはゴシック体1種類のみ（例: Noto Sans JP）。ウェイト違い
  （Regular / Bold）で階層を作る。
- 余白を広く取る。要素を詰めて埋めるより、削って空ける。
- 角丸・ドロップシャドウ・立体感・光沢は使わない。図形は直角、
  区切りは細い罫線で。

【表】
- ヘッダー行・項目名列の背景は薄いグレー(#f2f2f2)まで。ベタ塗りの
  濃色ヘッダーは禁止。
- 罫線は細いグレーの横線を基本にする。縦線は最小限。
- 最重要の列・行を強調するときは、背景色ではなく「太い黒罫線で囲む」。
- 前年比マイナスなどの注意値も赤字にしない。太字か「△」表記で示す。

【グラフ】
- 棒・折れ線・円、すべてグレーの濃淡と黒だけで描く。
- 系列の判別は色相ではなく明度で行う（例: 前期=中間グレー、当期=黒）。
- 時系列の棒グラフは、注目させたい期（通常は直近）だけを最も濃くし、
  他はグレーにする。全部同じ濃さに塗らない。
- 注釈（「5年間で約2.1倍」など）は、装飾的な吹き出しやイラスト矢印
  ではなく、細い引き出し線+小さめのテキストで「注釈」として置く。
- 目盛り線は薄いグレーで最小限。凡例はグラフ内かすぐ横に小さく。

【箇条書き・並列要素】
- アイコンは原則使わない。丸いアイコンサークル（特に色違いで3つ並べる
  もの）は禁止。チェックマーク・電球・歯車などの汎用アイコンも禁止。
- 並列する項目（事業一覧・強みなど）は、細い罫線で仕切ったテキスト
  ブロック、または「01 / 02 / 03」の番号付きリストで表現する。
- 項目ごとの説明文の長さを無理に揃えない。3行と5行が混在してよい。
  「見た目を揃えるための水増し・切り詰め」をしない。
- 「必ず3つにまとめる」ことをしない。項目数は内容の必然性で決める。
  2つなら2つ、5つなら5つのまま。

【表紙・扉】
- 表紙は「タイトル+日付+社名（+写真1枚）」まで。装飾図形で埋めない。
- 写真を使う場合は加工せずそのまま。写真の上に色付きの帯・図形を
  重ねない（白または黒の面で区切るのは可）。
- 幾何学的な切り込み（矢羽根など）を使う場合も、そこに色を乗せない。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 5. 禁止事項リストと最終セルフチェック
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

以下は「AIっぽさ」の典型パターンです。1つでも該当したら出力せず、
修正してから出力してください。

禁止事項:
- 青・水色・紫の同系色を複数同時に使う配色
- グラデーション（背景・図形・グラフ・帯、すべて）
- 色違いの丸アイコンを3つ横に並べる構成
- 角丸+ドロップシャドウの「カード」を等間隔に敷き詰める構成
- 濃色ベタ塗り+白抜き文字のテーブルヘッダー・強調セル
- 装飾目的の矢印イラスト・上昇イメージの半透明オブジェクト
- 影付き・色付きの吹き出し
- チェックマークアイコンの箇条書き
- 全項目を同じ行数・同じ文字量に無理に揃えた並列ブロック
- 同一ページ内での同じ数値・同じ主張の重複表示

生成直前の最終確認（毎回、出力に含めて自己申告すること）:
□ 使った色は 黒/白/グレー3種(+写真・ロゴ原色) だけか
□ グラデーション・角丸・影・アイコンサークルはゼロか
□ 強調は 太字/サイズ/黒罫線/グレー濃淡 だけで行ったか
□ グラフの系列は明度だけで判別できるか
□ 同じ情報を2か所で見せていないか
□ 項目数・行数を「揃えるため」に加工していないか
□ 数値・固有名詞は元データと照合済みか
```

---

## 使い方のヒント

- **元資料が画像しかない場合**: そのまま添付して構いません。構成案フェーズで
  読み取り結果が要約されるので、誤読があればそこで直せます。
- **コーポレートカラーをどうしても使いたい場合**: それでも一度この白黒版を
  作るのがおすすめです。白黒で成立するレイアウトに1色だけ足すのは簡単ですが、
  色に頼ったレイアウトから色を抜くと崩れます。
- **途中でAIっぽさが戻ってきたら**: 「■0と■5を再読してからやり直して」と
  指示すると効きます。長い対話ではルールの優先度が薄れがちなので、配色ルールを
  独立した最優先セクションにしてあるのはこのためです。
